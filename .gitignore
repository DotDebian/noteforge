node_modules
.nuxt
.nitro
.output
dist
.cache

# Excalidraw fonts, copied out of node_modules by scripts/copy-excalidraw-assets.mjs
# on `pnpm dev` / `pnpm build`. Generated, ~14 MB — not source.
public/excalidraw/

# env
.env
.env.local

# database / data
# `data/*.db` alone did not cover data/backups/, which holds full copies of the
# production database — real user data, tens of MB. Ignore the directory.
data/backups/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/uploads/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.swp

# Claude Code (local settings only)
.claude/settings.local.json
